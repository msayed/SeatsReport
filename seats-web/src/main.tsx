import React from 'react'
import ReactDOM from 'react-dom/client'
import SeatsReport from './SeatsReport'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SeatsReport />
  </React.StrictMode>
)
